import React, { useState, useEffect } from "react";
import "../css/Card.css";

const Card = ({ id, className }) => {
  return (
    <div id={id} className={className}>
    </div>
  );
};

export default Card;
